services:
  - type: web
    name: my-server
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: node dist/index.js
    installCommand: npm install --omit=optional
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
